1- Make sure that "truffle-config.js" network corresponds to "development"
2- run from truffle project folder the command : truffle develop
3- run "truffle test {java script file with tests}" (ex: truffle test ./test/Unit_Tests_CertificatePoolManager.js) or "truffle test" (to run all tests)
